import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    headerTitleStyle: {
        fontSize: 30,
        color: "#ff7106",
    },
    headerStyle: {
        backgroundColor: "#151e67",
    },
    emptyBuildingText: {
        fontSize: 30,
        color: "#000000",
        textAlign: "center",
    }
});